[package]
name = "vault-profiler"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
vault-engine = { path = "../../vault-engine" }

[dev-dependencies]
tempfile = "3"
